# Dipendenze PandaMem bot
# NB: NON installare il pacchetto "telegram" (e' un altro pacchetto e rompe python-telegram-bot)
python-telegram-bot[job-queue]>=21.0
requests
beautifulsoup4
pillow
# opzionali (non servono per /gpt, che usa Gemini via REST con requests)
# openai                   # solo se vuoi usare chatgpt.py / Openai_Dalle_E_img_model.py
# bing_image_downloader    # solo per /find (disattivato)
